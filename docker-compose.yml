services:
  app:
    container_name: english-summary-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - APP_PASSWORD=${APP_PASSWORD}
      - AUTH_SECRET=${AUTH_SECRET}
      - CRON_SECRET=${CRON_SECRET}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemini-3.6-flash}
      - PORT=${PORT:-3000}
      - HOSTNAME=${HOSTNAME:-0.0.0.0}
    # Bật dòng này nếu muốn ứng dụng đợi Postgres container sẵn sàng trước khi chạy migration:
    # depends_on:
    #   db:
    #     condition: service_healthy

  # Optional Postgres service (Mở comment nếu anh muốn Docker Compose dựng luôn Database)
  # db:
  #   image: postgres:16-alpine
  #   container_name: english-summary-db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER:-postgres}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
  #     POSTGRES_DB: ${POSTGRES_DB:-english_summary}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   postgres_data:

